# 任務計時器 · Mission Timer

單一檔案的 25 分鐘任務計時器。整個專案就是 `index.html` — HTML、CSS、JS、所有圖形全部內嵌，**沒有建置步驟、沒有相依套件**。

線上版：https://ellen-mission-timer.vercel.app
Repo：https://github.com/ellen-mooninite/Ellen-mission-timer

---

## 行為規範

**每次進到這個專案都要遵守。** 這是設計上的約定，不是建議 —— 要偏離請先問過，不要自己決定。

### 1. 深色星空背景，主色一次只強調一個元素

背景永遠是深色星空。主色（`--accent`）是稀缺資源，**同一時間只給「當下最該被注意的那一個元素」**，不要到處灑。

判斷方式是問「現在使用者該看哪裡」：

| 狀態 | 主色給誰 | 其餘 |
|---|---|---|
| 待命 | 發射鍵 | 一律灰階 |
| 推進中 | 進度（進度條 + 火箭） | 數字維持白色 |
| 最後一分鐘 | 全畫面升溫，這時**警示本身就是唯一焦點** | — |

新增元素時預設用 `--muted` 或灰階。想用主色，先確認它是不是真的搶得過現有的焦點；如果會分散注意力，就不要用。

### 2. 星球與火箭一律用 canvas 或 CSS 畫，不得引用外部圖片

**禁止任何 `<img>`、`background-image: url()`、外部 SVG 檔、字型圖示、CDN 素材。**

允許的做法只有三種：canvas 繪製、inline SVG、純 CSS。這條沒有例外 —— 包含「先暫時放張圖之後再換掉」也不行。

理由是這個專案的核心保證就是**單檔可攜、離線可跑、零相依**。目前整頁只有一個外部請求（日出日落 API），這個數字不應該增加。改動後可以用瀏覽器的 Network 面板確認：除了 HTML 本身和那支 API，不該有第三個請求。

### 3. 按鈕文案使用航太語彙

固定用這組詞彙，不要換成「開始 / 暫停 / 重設」這類一般 UI 用語：

| 詞 | 意義 |
|---|---|
| **發射** | 開始倒數 |
| **待機** | 暫停 |
| **返航** | 歸零重來 |
| **補給** | 延長時間 / 追加一段（目前尚未使用，保留給日後功能） |

需要新按鈕時，先從這組挑；這組沒有合適的，就照同樣的語氣造一個航太用詞，不要混入一般用語。

### 4. 倒數的分鐘數放在最上面當設定

時間相關的可調數值要集中成一組具名常數，**放在 script 最前面**，讓人不用讀完整份程式就能改。不要把分鐘數、秒數散在函式裡，也不要出現沒有名字的魔術數字。

目前實作在 `index.html` 的 script 開頭（第 0 區）：

```js
const MINUTES       = 25;   // 一次任務的長度（分鐘）
const WARN_MINS     = 1;    // 剩下幾分鐘開始警示（星球轉紅、岩漿噴發）
const CRITICAL_SECS = 10;   // 剩下幾秒開始閃爍數字

const TOTAL       = MINUTES       * 60 * 1000;
const WARN_MS     = WARN_MINS     * 60 * 1000;
const CRITICAL_MS = CRITICAL_SECS      * 1000;
```

人看得懂的單位（分、秒）放上面，換算成毫秒的衍生值緊接在下面；程式其餘地方只用 `TOTAL` / `WARN_MS` / `CRITICAL_MS`，不再出現任何時間的裸數字。

新增任何「使用者可能會想調」的數值（任務長度、警示門檻、日後的補給秒數）都放進同一區，不要另外散開。

---

## 本機開發

不能用 `file://` 直接開 — 日出日落的 API 請求會被 CORS 擋掉。起一個靜態伺服器：

```bash
cd "/Users/ellenyang/Desktop/Claudcode 0809" && python3 -m http.server 8731 --bind 127.0.0.1
```

然後開 http://127.0.0.1:8731/index.html

這台機器上**沒有 Node / npm / gh / Vercel CLI / Homebrew**，所以任何需要它們的做法都行不通。部署完全靠 `git push`（見下方）。

---

## 畫面結構

三層 canvas 疊在 UI 底下，z-index 由低到高：

| 層 | id | 內容 | 重畫時機 |
|---|---|---|---|
| 0 | `#sky` | 星空 + 正常配色的星球 | 只在 resize / 換星球 |
| 1 | `#sky-warn` | **同一個場景的紅色版**，用 opacity 疊上來 | 只在 resize |
| 2 | `#fx` | 岩漿粒子、熔岩貓 | 最後一分鐘每幀 |

UI（`.hud`）是 z-index 3，頁籤 4，`TIME'S UP` 是 5。

---

## 三個不能破壞的前提

動這個檔案之前先讀這一段，這些是效能與正確性的關鍵。

### 1. 粒子點陣一次只能畫一次

星球是約十萬顆粒子的經緯點陣（`paintScene()`），畫一次要數十毫秒。**絕對不要放進 requestAnimationFrame**。

所以「最後一分鐘變紅」不是重新著色粒子，而是預先把整個場景用紅色配色再畫一次到 `#sky-warn`，再靠 CSS opacity 交叉淡入。紅色版跟星球無關（只跟畫布尺寸有關），所以換星球不需要重畫。

紅色版是在 `requestIdleCallback` 的空檔預先算好的（`scheduleAlertRender()`），才不會在倒數剩 1 分鐘那一刻卡住畫面。

### 2. 星星座標是凍結的

`STARS` 在載入時用固定種子的 PRNG 產生一次，之後 `Object.freeze`。座標是 0~1 的正規化值，重畫時才乘上畫布尺寸。

**任何重畫（resize、換星球、進入警示）都不可以讓星星位移。** 這點有實測驗證過：抓畫面上半部亮點的座標集合，在五種星球之間切換後完全相同。改動繪圖流程後請重跑這個檢查。

### 3. 顏色不要從藍直接內插到紅

藍 → 紅的線性內插中間值是灰色，畫面會整片發灰、像褪色而不是加溫。`applyAccent()` 用兩段式：先升溫到橘（`HOT_MID`），後半段再燒到紅。

同理，UI 升溫（`warnLevel`，曲線 `^0.75`）比星球轉紅（`planetLevel`，曲線 `^1.9`）更早開始 — UI 早點提示，星球晚點才整顆燒起來。

---

## 計時器

- `TOTAL = 25 分鐘`、`WARN_MS = 最後 60 秒`
- 用**絕對終點時間** `endAt` 而不是每幀累加。分頁切到背景時 rAF 會被節流，累加會少算時間；`endAt` 不會。`visibilitychange` 回前景時補算一次。
- 三顆按鈕永遠是啟用狀態、發射永遠是實心主色（跟設計稿一致）。重複按發射是 no-op。

### 最後一分鐘的警示

強度全部由 `urgency`（0 → 1）連續驅動，沒有硬切：

- 星球逐漸轉紅、地平線浮出熾熱輝光
- 岩漿沿著地平線噴發，噴發量、速度、粒子大小都隨 `urgency` 增加
- 主色從星球色升溫到橘再到紅（進度條、火箭、發射鍵、數字一起）
- 火箭抖動幅度連續變大 — 用 JS 直接寫 transform，因為 CSS 動畫沒辦法連續調整振幅
- 分頁標題加上 `⚠` 前綴（在背景分頁時這是唯一看得到的警示）
- 最後 10 秒 `is-critical`，數字開始閃

### 歸零

`startDevil()` 觸發熔岩貓：從地平線後方**跳**出來（蓄力 → 彈射 → 過頂 → 落下 → 著地壓縮），然後 `TIME'S UP` 彈出。

貓被 `clip('evenodd')` 限制只畫在星球輪廓**之外**，所以看起來是從地平線後面竄出來的。進度條這時會淡出，不然線會橫切過貓臉。

---

## 日出日落

載入時打 Open-Meteo 拿當天台北的日出日落。

API 回的是 `"2026-08-09T05:25"`，**已經是 Asia/Taipei 當地時間**。直接取字串的 `HH:MM`，不要丟給 `new Date()` — 那個字串沒有時區標記，`Date` 會用瀏覽器所在時區去解讀，人在國外開就會顯示錯的時間。

任何失敗都顯示「離線」，不影響其他功能。已實測四種失敗情境：連線失敗、404、JSON 結構不對、時間字串是垃圾。另有 8 秒逾時。

這是整頁**唯一**的外部請求。除此之外沒有任何外部圖片、字型或 script。

---

## 無障礙

`prefers-reduced-motion` 下會關掉抖動、閃爍、脈動，但**紅色警示與熔岩貓依然會出現**（靜態版）。警示訊息不應該因為使用者的動態偏好而消失。

---

## 部署

`main` 推上去 Vercel 就會自動重新部署，不需要任何設定檔（純靜態，Vercel 認得 `Other`）。

```bash
git push origin main
```

**注意網址**：Vercel 那個帶亂碼的單次部署網址（`...-60jodqajt-...`）有登入保護，分享給別人只會看到 Vercel 登入頁。要分享請用正式網址 `ellen-mission-timer.vercel.app`。

GitHub 用 SSH 金鑰推送（`~/.ssh/id_ed25519`，無密碼短語）。

---

## 改動後的驗證清單

沒有自動化測試，用瀏覽器實際跑過這幾項：

1. Console 沒有錯誤；網路請求只有 HTML + Open-Meteo
2. 五個星球都切得動，星星沒有位移
3. 發射 / 待機 / 返航 三個狀態正確
4. 最後一分鐘：顏色連續升溫（中間不能發灰）、岩漿變密、火箭抖動、標題有 ⚠
5. 歸零：貓跳出來、`TIME'S UP` 出現、進度條淡出
6. 歸零後按返航 / 發射，特效要完全清乾淨（`stopDevil()`）
7. 手機寬度版面不破

要測警示與歸零不必等 25 分鐘 — 複製一份到暫存目錄，把 `TOTAL` 和 `WARN_MS` 改小再跑。**改暫存副本，不要改原檔**。
